<% if(theme.notification && !(url_for(page.path) != '/index.html' && url_for(page.path).indexOf('categories') == -1)){ %>
    <div class="widget notification">
        <h3 class="title">网站公告</h3>
        <div>
            <%- theme.notification%>
        </div>
    </div>
<% } %>